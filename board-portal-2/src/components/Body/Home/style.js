import { makeStyles, withStyles } from "@material-ui/core/styles"

export const useStyles = makeStyles(theme => ({
  root: {},
  bodyPage: {
    margin: "4%",
    paddingTop: "0.5em",
    paddingLeft: "2em",
    paddingBottom: "56px"
    // height: "100vh"
  }
}))
